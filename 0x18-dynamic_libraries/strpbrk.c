#include "main.h"
/**
 *_strpbrk - Finds first character in string
 *that matches any character in accept
 *@s: string to be scanned
 *@accept: has characters to match
 *
 *Return: Null if unsuccessful,
 *pointer to character in s if successful
 */
char *_strpbrk(char *s, char *accept)
{
	return (s);
}
