#include "main.h"
/**
 *_strstr - finds first occurrence of substring in string
 *@haystack: string to be scanned
 *@needle: substring to be matched
 *
 *Return: Null if no match, pointer to first occurrence
 *of substring in string if successful
 */
char *_strstr(char *haystack, char *needle)
{
	return (haystack);
}
