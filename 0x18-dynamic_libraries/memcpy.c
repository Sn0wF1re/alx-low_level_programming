#include "main.h"
/**
 *_memcpy - copy block of memory from one location
 *to another
 *@src: source of characters to be copied
 *@dest: destination after copying
 *@n: number of bytes to be copied
 *
 * Return: copied characters
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (dest);
}
